# One-paste Goal

Run `node bin/dw.mjs run <plan>` until the transcript contains `DW_RUN_COMPLETE`; if any step fails, inspect `dw review <run-id>`, fix only the failing criterion, and rerun or resume from durable state.
